import {MoodleWsInterceptor} from './moodle-ws.interceptor';

describe('MoodleWsInterceptor', () => {
  it('should create an instance', () => {
    expect(new MoodleWsInterceptor()).toBeTruthy();
  });
});
